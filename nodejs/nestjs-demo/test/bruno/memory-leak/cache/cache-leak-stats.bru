meta {
  name: cache-leak-stats
  type: http
  seq: 3
}

get {
  url: {{host}}/memory-leak/cache/stats
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.size: isNumber
  res.body.memoryUsage: isString
  res.body.maxSize: isNumber
}
