meta {
  name: closure-leak-start
  type: http
  seq: 1
}

post {
  url: {{host}}/memory-leak/closure/start
  body: none
  auth: none
}

assert {
  res.status: eq 201
  res.body.message: isDefined
  res.body.stats: isDefined
  res.body.stats.activeClosures: isNumber
  res.body.stats.totalMemoryAllocated: isNumber
  res.body.stats.isLeaking: isTruthy
}
