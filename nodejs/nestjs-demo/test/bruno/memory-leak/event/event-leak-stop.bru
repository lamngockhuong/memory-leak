meta {
  name: event-leak-stop
  type: http
  seq: 4
}

post {
  url: {{host}}/memory-leak/event/stop
  body: none
  auth: none
}

assert {
  res.status: eq 201
  res.body.message: isDefined
  res.body.stats: isDefined
  res.body.stats.activeListeners: eq 0
  res.body.stats.totalMemoryAllocated: eq 0
  res.body.stats.isLeaking: isFalsy
}
